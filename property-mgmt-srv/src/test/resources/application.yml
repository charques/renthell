spring:
  data:
    mongodb:
      uri: mongodb://192.168.99.100:27017/testdb

endpoints:
  event-store:
    uri-base: http://192.168.99.100:8090
    add-property-command: /commands/add-property-transaction

kafka:
  bootstrap-servers: ${spring.embedded.kafka.brokers}
  groupid:
    events: events-group
  topic:
    events: test
